# Repo-wide ruff configuration (services + scripts are Python 3.11).
target-version = "py311"
line-length = 100

[lint]
# Pin the rule set explicitly so upstream default changes never break CI:
# pycodestyle errors, pyflakes, and import sorting.
select = ["E4", "E7", "E9", "F", "I"]
