# Serving dependencies only — training deps live in finetune/requirements.txt
# so the Docker image stays slim.
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
psycopg[binary]>=3.1,<4.0
psycopg-pool>=3.2,<4.0
huggingface_hub>=0.24,<1.0
